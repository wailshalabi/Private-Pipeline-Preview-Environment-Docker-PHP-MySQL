# Copy this file to .env and fill in values.

# Used by Woodpecker server+agent. Any random string works.
WOODPECKER_AGENT_SECRET=change-me-to-a-random-string

# Create an OAuth2 application in Gitea:
#   Settings (top-right) -> Applications -> Create OAuth2 Application
# Use Redirect URI: http://ci.localhost/authorize
WOODPECKER_GITEA_CLIENT=your-client-id
WOODPECKER_GITEA_SECRET=your-client-secret

# Shared secret for the Gitea webhook that triggers cleanup on PR close/merge.
PREVIEW_WEBHOOK_SECRET=change-me-too
